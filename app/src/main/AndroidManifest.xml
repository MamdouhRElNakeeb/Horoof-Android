<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="agency.crete.horoof">

	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
	<uses-permission android:name="android.permission.INTERNET" />


    <application
		    android:name=".helper.App"
		    android:allowBackup="true"
		    android:icon="@mipmap/ic_launcher"
		    android:label="@string/app_name"
		    android:roundIcon="@mipmap/ic_launcher_round"
		    android:supportsRtl="true"
		    android:usesCleartextTraffic="true"
		    android:theme="@style/AppTheme"
		    android:networkSecurityConfig="@xml/network_security_config"
		    tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".view.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.Home"/>
        <activity android:name=".view.Help"/>
	    <activity android:name=".view.Settings"/>
	    <activity android:name=".view.Leaderboard"/>
	    <activity android:name=".view.NewContest"/>
	    <activity android:name=".view.Categories"/>
	    <activity android:name=".view.Login"/>
	    <activity android:name=".view.Register"/>
	    <activity android:name=".view.ContestPlay_TurnsMethod"/>
	    <activity android:name=".view.ContestPlay_HoroofMethod"/>

    </application>

</manifest>